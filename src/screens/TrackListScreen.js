import React from "react";
import { Text, StyleSheet, View } from "react-native";

const TrackListScreen = () => {
  return <Text style={{ fontSize: 40 }}>TrackListScreen</Text>;
};

const styles = StyleSheet.create({});

export default TrackListScreen;
