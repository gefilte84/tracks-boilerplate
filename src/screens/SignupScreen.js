import React from "react";
import { Text, StyleSheet, View } from "react-native";

const SignupScreen = () => {
  return <Text style={{ fontSize: 40 }}>SignupScreen</Text>;
};

const styles = StyleSheet.create({});

export default SignupScreen;
